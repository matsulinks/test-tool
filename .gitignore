# 依存パッケージ
node_modules/

# 環境変数（秘密情報）
.env

# 証拠写真（テスト失敗時の一時ファイル）
tests/registry/evidence/

# 失敗カウンター（一時ファイル）
tests/registry/.fail_count

# 報告書（使い終わったら消えるもの）
tests/registry/latest_report.md
tests/registry/grok_question.md

# macOS
.DS_Store
