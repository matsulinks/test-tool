# @docs/spec.md (設計図) - Alchemist Test Pilot (ATP)

## 0. 哲学 / Philosophy
**「テストは、コードの正しさを証明するためだけにあるのではない。開発者が安心して、限界を超えた創造に没頭するための『聖域』を守るためにある。」**

AIがコードを書く現代において、人間が細かなバグに怯える時間は最大の損失である。本ツールは、開発者の「五感」の代行者となり、常に背後で「聖域」が侵されていないかを監視し続ける。もし聖域が崩れたならば、それを即座に黄金（修正）へと変えるための道標（エビデンス）を提示する。

---

## 1. App Name / Visual Persona
**Alchemist Test Pilot (ATP)**
> **🛰️ 📋 🛡️**
> （高所からの監視、詳細な報告書、聖域を守る盾）

---

## 2. Concept (比喩を用いた説明)
**「開発という飛行を、目隠しをしても完遂させるための『自動操縦士』」**

単にテストを回すプログラムではない。開発レジストリ（作り手）と対話し、たとえ視界（UIやAPIの挙動）が荒れても、正確な計器（ログ・スクショ）を見て航路を修正し続ける専門家である。

---

## 3. 言葉の定義
*   **開発レジストリ**: `vault-alchemist/`（修正を行う側のAIの戦場。GitHub: matsulinks/vault-alchemist）
*   **テストレジストリ**: 本ツール（ATP）が管理するレポート・証拠・AIへの示唆を収める場所（AIの教育係）
*   **聖域（Sanctuary）**: すでに合格したテスト項目。ここが一つでも崩れることは「退行（Regression）」として絶対に許さない。
*   **ベースライン（基準状態）**: テストを開始する前の「正常な状態」の定義。**開発レジストリ側（vault-alchemistのdocs/spec.md）で定義・管理する責任を持つ。**

---

## 4. 主要機能 (Main Features)

### 4.1. 高解像度監視 (High-Res Monitoring)
*   **CLI検証**: `npm run test` 等の実行結果をリアルタイム監視し、53項目の生死を判定。
*   **UI/API検証**: Obsidianを自動操作し、APIの戻り値と画面の状態を「証拠（スクリーンショット）」と共に記録。
*   **コスト方針**:
    *   **原則**: AIへはテキストの報告書を読ませる（Vision APIは使わない）。
    *   **例外（UI問題）**: 「5px右にずらせばいい」など言葉で正確に伝えられる場合はテキストのみ。言葉での表現が難しいレイアウト崩れ・見た目の問題は、スクショを添付して判断を助ける。
    *   **判断基準**: 「言葉で十分に伝わるなら言葉で。迷ったらスクショで。」その場の開発AIに判断を委ねる。

### 4.2. 自動修復連携 (Repair Loop)
*   テスト失敗時、AIがそのまま読み込める形式で「テストレジストリ」へ失敗報告を出力。
*   **1回目・2回目の失敗**: 開発レジストリ側のAIへ修正を促す指示を作成。
*   **3回目の失敗（デッドロック）**: 自力での修復を断念し、Grok (xAI) への質問状（ドラフト）を自動作成。監督に判断を仰ぐ。

### 4.3. 安全装置 (Safety Guards)
*   **実行リミット**: 修正トライは最大**3回**。3回で改善しない場合は「デッドロック」として停止する。
*   **聖域の再検証**: 修正後、ターゲット以外の既存テスト（53項目等）も必ず全件通過することを確認。

### 4.4. 使い捨て報告書（チケット制）
*   **運用**: テスト報告書（`latest_report.md`）は、修正担当AIが修正をすべて完了し、次のテスト（答え合わせ）を受け入れる直前に削除する。
*   **哲学**: **「使い終わったら消す」**。報告書はその目的（修正の完了）を果たした瞬間に役目を終える。修正中は全情報を手元に置き、完了後に潔く手放す。

### 4.5. ゼロ摩擦セットアップ（One-Paste Setup）
*   **運用**: セットアップスクリプト（`scripts/setup.sh`）を1回実行するだけで環境が整う。
*   **監督がやること**: ブラウザでGitHubトークンを発行し、コピーする。スクリプトが「貼り付けてください」と表示したら**ペースト1回 → Enter**のみ。
*   **ATPが自動でやること**: トークンの`~/.zshrc`への書き込み、`.env`の生成、`.gitignore`への追加、GitHub接続確認まで一括実施。

### 4.6. GitHub Issue連携（方法B採用）
*   **テスト失敗時**: ATPが`vault-alchemist`リポジトリに「🔴 テスト失敗」のIssueを自動投稿。エラー詳細・証拠ログを添付。
*   **修正完了時**: 開発AIがIssueをCloseする。これがATPへの「再テスト開始」の合図となる。
*   **記録効果**: 全ての修正履歴がGitHub Issueとして永続的に蓄積される。

### 4.7. プロジェクトのライフサイクル管理（Project Lifecycle）
*   **誕生**: 開発レジストリがGitHub Issue で「ATPペアリング依頼」を送る。ATPが `tests/config/[project].yaml` を生成。
*   **活動**: 開発続行中、ATPはそのYAMLを基に定期テストを実施。
*   **終了**: 開発AIがGitHub Issueに `atp:project-complete` タグを付ける。ATPが検知し、YAMLを `tests/archive/[project]_YYYYMMDD.yaml` へ自動移動。以降そのプロジェクトのテストはスキップ。
*   **哲学**: 「使い終わったら消す」の哲学を、プロジェクト全体のライフサイクルにまで拡張したもの。記録（archive）は残すが、活動の負荷は積まない。


```text
（テスト環境構築）/                   ← ATPのルート（本ツール全体の家）
│
├── docs/                             ← 頭脳（考えたことを全部書く場所）
│   ├── spec.md                       ← 設計図（何を作るか）
│   ├── tasks.md                      ← 作業手順書（どう進めるか）
│   ├── decision_log.md               ← 決定事項と哲学の記録（なぜそうしたか）
│   └── backlog.md                    ← あとで考えることリスト
│
└── tests/                            ← 操縦室（テストを動かす場所）
    │
    ├── core/                         ← エンジン（テストを実際に動かすプログラム）
    │   └── monitor.js                ← 見張り番（テスト実行→結果を報告書に書く）
    │
    ├── config/                       ← レシピ棚（各プロジェクトのテスト定義）
    │   ├── self/                     ← 自分用レシピ棚
    │   │   └── obsidian.yaml         ← vault-alchemist のテスト62項目
    │   └── clients/                  ← 請負用レシピ棚（将来のクライアント分）
    │
    ├── archive/                      ← 倉庫（完了したプロジェクトのレシピを保管）
    │
    └── registry/                     ← 郵便受け（AI同士のやり取りをここで行う）
        ├── latest_report.md          ← テスト報告書（使い終わったら消す！）
        └── evidence/                 ← 証拠写真置き場（スクリーンショット等）

※ /tmp/atp_sandbox/                   ← 一時的な作業台（テスト後に自動消去）
```

---

## 6. Change Log (変更履歴)
*   2026/02/26: 初版策定。哲学・Grok連携・聖域・チケット制の概念を定義。
*   2026/02/26: レビューに基づき構造修正。修復回数を3回に統一。「ベースライン」の責任先を明確化。哲学の言葉を「使い終わったら消す」に精緻化。
*   2026/02/26: 「プロジェクトのライフサイクル管理」を追加。「使い終わったら消す」哲学をプロジェクトレベルに拡張。archive/フォルダも設計に組み込む。

---

## 7. この設計の致命的な弱点 / Critical Flaw
**「テストコード自体のバグに弱い」**
ATPが読み込んでいる「期待される結果」自体が間違っていた場合、AIは「正しいコードを間違っていると見なし、壊し続ける」リスクがある。そのため、テスト定義の修正には監督の承認が必須である。
